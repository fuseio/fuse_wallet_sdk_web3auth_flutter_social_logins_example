name: fuse_wallet_sdk_web3auth_flutter_example
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.1.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  fuse_wallet_sdk:
    git:
      url: https://github.com/fuseio/fuse_wallet_sdk.git
      ref: feat/erc-4337
  web3auth_flutter: ^2.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
